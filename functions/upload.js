export async function onRequestPost({request,env}){const form=await request.formData();const file=form.get('file');if(!file||!file.name)return Response.json({ok:false,error:'No file'},{status:400});const key=`documents/${Date.now()}-${crypto.randomUUID()}-${file.name.replace(/[^a-zA-Z0-9._-]/g,'_')}`;await env.UPLOADS.put(key,file.stream(),{httpMetadata:{contentType:file.type||'application/octet-stream'}});await env.DB.prepare(`INSERT INTO uploads (id,entity_type,entity_id,r2_key,file_name,created_at) VALUES (?,?,?,?,?,datetime('now'))`).bind(crypto.randomUUID(),'document','library',key,file.name).run();return Response.json({ok:true,key});}