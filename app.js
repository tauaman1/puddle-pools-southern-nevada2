const menu=document.querySelector('#menu'),nav=document.querySelector('#nav');menu?.addEventListener('click',()=>nav.classList.toggle('open'));document.querySelectorAll('nav a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));
async function submitForm(form,status,url){status.textContent='Submitting…';const data=Object.fromEntries(new FormData(form).entries());try{const r=await fetch(url,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});if(!r.ok)throw new Error();status.textContent='Thank you — your request was submitted.';status.className='status ok';form.reset()}catch(e){status.textContent='Demo mode: form captured locally. Connect the included Cloudflare Function + D1 binding to make submissions live.';status.className='status';localStorage.setItem('last_'+form.id,JSON.stringify({...data,submitted_at:new Date().toISOString()}));}}
const q=document.querySelector('#quoteForm');q?.addEventListener('submit',e=>{e.preventDefault();submitForm(q,document.querySelector('#quoteStatus'),'/api/quote')});const c=document.querySelector('#contactForm');c?.addEventListener('submit',e=>{e.preventDefault();submitForm(c,document.querySelector('#contactStatus'),'/api/contact');});